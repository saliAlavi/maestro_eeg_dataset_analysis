"""transfer_ssl: whole-trial spatial+gaze decoder with cross-subject transfer and
masked-channel SSL pretraining. Run via the `transfer_ssl` runner (not the per-window
AAD runner). See nets.py for the encoder/classifier and runner for the protocol."""
