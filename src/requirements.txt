# Installed in the `nips` conda env (python 3.11). Pinned-ish for reference.
# Core present in env: torch>=2.10, numpy>=2.2, scipy, pandas, scikit-learn>=1.7,
# mne>=1.10, librosa, soundfile, matplotlib, pyarrow, cv2 (opencv), tqdm,
# wandb>=0.25, hydra-core>=1.3, omegaconf>=2.3
# Added for this framework:
pyriemann>=0.11
# Optional (future: SSL speech / EEG foundation features):
# torchaudio
# transformers
